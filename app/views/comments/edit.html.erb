<h2>Edit Comment</h2>

<%= form_with model: [@item, @comment], local: true do |f| %>
  <p>
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </p>

  <%= f.submit "Update Comment" %>
<% end %>

<br>

<%= link_to "Back", item_path(@item) %>